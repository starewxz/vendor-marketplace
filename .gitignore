# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Env files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
logs/

# Testing
coverage/

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Docker volumes data (if ever created locally)
.data/

# Load test generated artifacts (contain live tokens / raw run output)
load-tests/.load-test-env.json
load-tests/load-test-result.json

# One-time local symlink to backend/node_modules so multi-instance-realtime-check.mjs
# can resolve socket.io-client — see README "Multi-instance verification"
load-tests/node_modules

# Real Kubernetes secret filled in from k8s/secret.example.yaml — never commit
k8s/secret.yaml
