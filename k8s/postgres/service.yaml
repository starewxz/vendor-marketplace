apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: marketplace
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None # headless — stable DNS for the StatefulSet's single pod
